<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 按下 -->
    <item android:drawable="@mipmap/tab_sy_selected" android:state_pressed="true"/>

    <!-- 选中 -->
    <item android:drawable="@mipmap/tab_sy_selected" android:state_checked="true"/>

    <!-- 其他 -->
    <item android:drawable="@mipmap/tab_sy_normal"/>

</selector>